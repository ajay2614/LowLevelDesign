package Patterns.MVCDesignPattern;

public class MVCImpl {
    /**
     * MVC, a design pattern used almost everywhere
     * M = Model (POJO Files connected with DB etc) without any business logic files
     * V = View (Main Business Logic)
     * C = Controller (Frontend, no business Logic, just the UI etc logic)
     * */
}
